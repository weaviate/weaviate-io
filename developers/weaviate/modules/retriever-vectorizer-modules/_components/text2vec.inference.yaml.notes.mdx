import Admonition from '@theme/Admonition';

export default function Text2vecInferenceYamlNotes(props) {
  return (
    <div>
      <Admonition type="note">
        <ul>
          <li>You can also use the <a href="/developers/weaviate/installation/docker-compose#configurator">Weaviate configuration tool</a> to create a Weaviate setup with this module.</li>
          <li>Starting with <code>v1.11.0</code> the <code>{props.apiname}</code> variable is now optional and you can instead provide the key at insert/query time as an HTTP header (<a href="#how-to-use">see below for instructions on how</a>)</li>
        </ul>
      </Admonition>
    </div>
  );
}
