import Admonition from '@theme/Admonition';

export default function DocDeprecationNote({ tagname, client_examples }) {
  const url = `/developers/weaviate/api/rest#${tagname}`;
  return (
    <div>
      <Admonition type="caution">
        This section of the documentation is deprecated and will be removed in the future.
        {tagname && (
          <div>Refer to the {``}<a href={url} target="_blank" rel="noreferrer">OpenAPI documentation</a> for the most up-to-date information.
          </div>
        )}
        {client_examples && (
          <div><br/>For client examples, see {``}
            <a href={client_examples} target="_blank" rel="noreferrer">this</a> section.
          </div>
        )}
      </Admonition>
    </div>
  );
}
