---
title: Bring your own vectors
sidebar_position: 40
image: og/docs/tutorials.jpg
# tags: ['getting started']
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

Weaviate is a vector database. [Vector databases](/blog/vector-library-vs-vector-database) store data objects and vectors that represent those objects. When you import a data object, you normally include a vector representation of that object as well. The vector representation is also called an "embedding."

This guide discusses importing data that already has vectors. An alternative approach is to use a [vectorizer](../modules/retriever-vectorizer-modules/index.md) model to generate vectors at import time. Weaviate has [integrations](/developers/weaviate/model-providers) that support most popular models.

To import data with your own vectors, follow these steps:

1. Create a Weaviate instance
2. Connect to the Weaviate instance
3. Import data that specifies its own vectors
4. Perform a vector search with Weaviate

## Setup

This section introduces the example data and setup requirements.

### Example Data

The example dataset is 10 questions from the popular quiz show "Jeopardy!". Each of the questions has an associated vector embedding.

In this example, these vectors were obtained through an OpenAI API, using the <nobr>[`text-embedding-ada-002` model](https://platform.openai.com/docs/guides/embeddings/what-are-embeddings)</nobr>. But your vectors can come from any source, such as another inference service provider (e.g Cohere or Hugging Face) or your own fine-tuned model.

<details>
  <summary>Take a look at the dataset</summary>

  |    | Category   | Question                                                                                                          | Answer                  | Vector                                             |
  |---:|:-----------|:------------------------------------------------------------------------------------------------------------------|:------------------------| ---------------------------------------------------|
  |  0 | SCIENCE    | This organ removes excess glucose from the blood & stores it as glycogen                                          | Liver                   | [ -0.006632288, -0.0042016874, ..., -0.020163147 ] |
  |  1 | ANIMALS    | It's the only living mammal in the order Proboseidea                                                              | Elephant                | [ -0.0166891, -0.00092290324, ..., -0.032253385 ]  |
  |  2 | ANIMALS    | The gavial looks very much like a crocodile except for this bodily feature                                        | the nose or snout       | [ -0.015592773, 0.019883318, ..., 0.0033349802 ]   |
  |  3 | ANIMALS    | Weighing around a ton, the eland is the largest species of this animal in Africa                                  | Antelope                | [ 0.014535263, -0.016103541, ..., -0.025882969 ]   |
  |  4 | ANIMALS    | Heaviest of all poisonous snakes is this North American rattlesnake                                               | the diamondback rattler | [ -0.0030859283, 0.015239313, ..., -0.021798335 ]  |
  |  5 | SCIENCE    | 2000 news: the Gunnison sage grouse isn't just another northern sage grouse, but a new one of this classification | species                 | [ -0.0090561025, 0.011155112, ..., -0.023036297 ]  |
  |  6 | SCIENCE    | A metal that is "ductile" can be pulled into this while cold & under pressure                                     | wire                    | [ -0.02735741, 0.01199829, ..., 0.010396339 ]      |
  |  7 | SCIENCE    | In 1953 Watson & Crick built a model of the molecular structure of this, the gene-carrying substance              | DNA                     | [ -0.014227471, 0.020493254, ..., -0.0027445166 ]  |
  |  8 | SCIENCE    | Changes in the tropospheric layer of this are what gives us weather                                               | the atmosphere          | [ 0.009625228, 0.027518686, ..., -0.0068922946 ]   |
  |  9 | SCIENCE    | In 70-degree air, a plane traveling at about 1,130 feet per second breaks it                                      | Sound barrier           | [ -0.0013459147, 0.0018580769, ..., -0.033439033 ] |

</details>


### Weaviate Instance

Weaviate is open source. You can [run Weaviate](/developers/weaviate/installation) locally, in the cloud, or as a service. If you want to follow this guide without setting up an instance of your own, consider using a free, Sandbox instance in [Weaviate Cloud]/developers/wcs/quickstart).

### Client library

[Client libraries](/developers/weaviate/client-libraries) simplify working with Weaviate. Clients are available for multiple programming languages. This guide provides examples in Python, Typescript, and cURL.

To install a client library, use the installer for the client language:

<Tabs groupId="languages">
<TabItem value="py" label="Python Client v4">
The v4 client requires Weaviate 1.23 or higher.<br/><br/>

```bash
pip install -U weaviate-client
```
</TabItem>
<TabItem value="py3" label="Python Client v3">

```bash
pip install "weaviate-client==3.*"
```
</TabItem>
<TabItem value="js" label="JS/TS Client v3">

```bash
npm install weaviate-client --tag next
```
</TabItem>
<TabItem value="js2" label="JS/TS Client v2">

```bash
npm install weaviate-ts-client
```
</TabItem>
</Tabs>

## Connect to Weaviate

To connect to your Weaviate instance, you need the following information:

- The Weaviate URL
- The Weaviate API key

If you are using a Sandbox instance, the URL and API keys are listed in the [details panel](/developers/wcs/quickstart#explore-the-details-panel) for your instance.

To connect to Weaviate, run the code for your language to create a client object. Re-use the `client` object in the later steps.

import ConnectToWeaviate from '/_includes/code/quickstart/connect.noheader.mdx'

<ConnectToWeaviate />

## Collection definition

Weaviate stores [data](../concepts/data.md) in collections. Each data object in a collection has a set of properties and a vector representation. Before you import data, you should create a [collection definition](/developers/weaviate/config-refs/schema) to define the collection properties.

The data property definitions are called a schema. Vectorizers are also defined at the collection level. Details, such as the specific model, or vectorization behavior, are defined at the property level.

This is a guide to using your own vectors. It assumes you already have vector embeddings for your data. The sample configuration code sets the vectorizer to `none` because the vector embeddings are included with the import file.

For more on configurations, see [specify]vectorizer settings](/developers/weaviate/manage-data/collections#specify-vectorizer-settings).

### Define the collection

import CreateSchema from '/_includes/code/quickstart.byov.schema.mdx'

<CreateSchema />

### Optional: Set a compatible vectorizer
If Weaviate has an [integration](/developers/weaviate/model-providers) for the vectorizer that you use to generate you custom vectors, consider adding your vectorizer to the collection definition.  If you specify a vectorizer, Weaviate can generate new vectors when it needs them.<br/>

In this example, the vectors are generated by an OpenAI model, `ada-002`. Weaviate provides an OpenAI integration, `text2vec-openai`, that can access the `ada-002` model so you would specify `text2vec-openai` in the collection definition.

Weaviate uses the vectors you provide at import even if the collection specifies a vectorizer.

## Import data and vectors

The example data is based on a set of ten questions from the "Jeopardy!" television program. [The data file](https://raw.githubusercontent.com/weaviate-tutorials/quickstart/main/data/jeopardy_tiny_with_vectors_all-OpenAI-ada-002.json) is a JSON formatted document that contains the vectors and the underlying data.

For large datasets, batch import is more efficient than importing individual objects. This [batch import](/developers/weaviate/tutorials/import#batch-import-process) code imports the question objects, including their vectors.

import ImportQuestionsWithVectors from '/_includes/code/quickstart.import.questions+vectors.mdx'

<ImportQuestionsWithVectors />

:::tip Vectors are not object properties
When you specify the elements of your batch import, be careful not to mislabel vectors as properties. If the vector is listed in the properties section, Weaviate processes it as a regular property, rather than as a vector embedding.
:::

## Query

In order to query a vector, Weaviate needs a vector representation of the query. If the collection definition specifies a vectorizer, Weaviate uses that vectorizer to generate a vector embedding for the query. The example collection in this guide does not specify a vectorizer. Since Weaviate cannot vectorize the query, you have to supply a query vector.

For example, to search the database for questions related to biology, use the same vectorizer that you used to create the data embedding to vectorize "biology" (the query term). Since the sample vectors are created with the `ada-002` model, the query ("biology") is also vectorized using the `ada-002` model.

This [`nearVector`](/developers/weaviate/search/similarity#search-with-a-vector) query use the vector embedding of "biology" to search the database:

import NearVector from '/_includes/code/quickstart.query.nearVector.mdx';

<NearVector />

You should see something like this:

import BiologyQuestionsJson from '/_includes/code/quickstart/response.biology.questions.mdx';

<BiologyQuestionsJson />

## Recap

If you made it here - well done. We have covered a lot, and you've successfully built a fully functioning vector database! 🥳

You have:
- Populated your Weaviate instance with your own vectors, and
- Performed text similarity searches.

Of course, there is a lot more to Weaviate that we have not yet covered, and probably a lot that you wish to know about. So we include a few links below that might help you to get started in your journey with us.

Also, please feel free to reach out to us on our community [Slack](https://weaviate.io/slack). We love to hear from our users.
