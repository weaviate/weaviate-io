---
title: Collections tool
sidebar_position: 15
image: og/wcs/user_guides.jpg
---

import WCDEdGen from '/developers/wcs/img/wcd-coll-ed-general.jpg';
import WCDEdProp from '/developers/wcs/img/wcd-coll-ed-props.jpg';
import WCDEdExProp from '/developers/wcs/img/wcd-coll-ed-extra-props.jpg';
import WCDEdSelect from '/developers/wcs/img/wcd-coll-select-mod.jpg';
import WCDEdObj from '/developers/wcs/img/wcd-coll-ed-objs.jpg';

The Weaviate Cloud (WCD) Collections tool makes it easy for developers and non-technical users to create, manage, and delete collections.

Use the tool to precisely tune a new collection. Specify a vectorizer and an embedding model. Configure collection properties for faster searches and more accurate results.

## Open a collection {#collection-details}

The collections tool is available for collections that are hosted in Weaviate Cloud.

import CollectionDetails from '/developers/wcs/img/weaviate-cloud-collection-details.png';

<ol>
  <li>
    To begin, select the <code>Collections</code> tool from the left sidebar (
    <span class="callout">1</span>).
  </li>
  <li>
    The <code>Clusters</code> panel lists the clusters that are available.
  </li>
  <li>
    Select a cluster from the list to create a new collection or to see the
    collections that are already in the cluster (<span class="callout">2</span>
    ).
  </li>
  <li>
    Click on the <code>Details</code> button (<span class="callout">3</span>).
  </li>
</ol>

<div class="row">
  <div class="card">
    <div class="card__image">
      <img src={CollectionDetails} alt="Open a collection in Weaviate Cloud" />
    </div>
    <div class="card__body">
      <p>Open a collection in Weaviate Cloud.</p>
    </div>
  </div>
</div>
<br />

If you don't see any available clusters, select the `Clusters` option from the left hand menu and follow the steps to [create a new cluster](/developers/wcs/manage-clusters/create).

## Create a collection

The `Create collection` button opens the collection editor. Click the button to create a new collection.

import CreateCollection from '/developers/wcs/img/weaviate-cloud-create-collection.png';

<ol>
  <li>
    To begin, select the <code>Collections</code> tool from the left sidebar (
    <span class="callout">1</span>).
  </li>
  <li>
    The <code>Clusters</code> panel lists the clusters that are available.
  </li>
  <li>
    Select a cluster from the list to create a new collection or to see the
    collections that are already in the cluster (<span class="callout">2</span>
    ).
  </li>
  <li>
    Click on the <code>Create collection</code> button (
    <span class="callout">3</span>).
  </li>
</ol>

<div class="row">
  <div class="card">
    <div class="card__image">
      <img
        src={CreateCollection}
        alt="Create a new collection in Weaviate Cloud"
      />
    </div>
    <div class="card__body">
      <p>Create a new collection in Weaviate Cloud.</p>
    </div>
  </div>
</div>
<br />

### Edit the General section

<div class="row">
  <div class="col col--6">
    <p>
      Use the <code>General</code> section for collection-wide settings:
    </p>
    <ol>
      <li>Name the collection</li>
      <li>Enable multi-tenancy</li>
      <li>Set a default vectorizer</li>
    </ol>
  </div>
  <div class="col col--6">
    <div class="card">
      <div class="card__image">
        <img src={WCDEdGen} alt="General collection settings" />
      </div>
      <div class="card__body">General collection settings.</div>
    </div>
  </div>
</div>
<br />

#### Name the collection

The collection name must begin with a capital letter.

The name can only contain letters, numbers, and the underscore (\_) character. Spaces are not allowed.

#### Enable multi-tenancy

[Multi-tenant collections](/developers/weaviate/manage-data/multi-tenancy) are useful if you have multiple users or need data isolation. By default, collections are single tenant. Set the `Multi-tenancy` toggle to enable multi-tenancy.

#### Set a default vectorizer

Set a [vectorizer](/developers/weaviate/config-refs/schema#vectorizer) for the collection. Weaviate uses the default vectorizer when you import unvectorized data and when Weaviate queries external APIs that require vectorized inputs.

The `Vectorizer` setting is a drop-down list. After you select a vectorizer, the editor expands to let you configure the model. The tool only shows the configuration options that are relevant for the vectorizer that you select.

Switch the `Vectorize class name` toggle if you don't want to create a vector for the class name.

### Edit the Properties section

Use the `Properties` section to fine-tune individual properties in your collection.

#### Add properties to the collection

Enter property names into the properties editor.

<div class="row">
  <div class="col col--4">
    <p>The property names must begin with a lowercase letter.</p>
    <br />
    <p>
      The names can only contain letters, numbers, and the underscore (\_)
      character. Spaces are not allowed.
    </p>
  </div>
  <div class="col col--8">
    <div class="card">
      <div class="card__image">
        <img src={WCDEdProp} alt="Properties edit section" />
      </div>
      <div class="card__body">Properties edit section.</div>
    </div>
  </div>
</div>
<br />

#### Configure additional details

Some property types can be fine-tuned. When additional configuration is possible, the editor shows a gear wheel next to the `Filterable` toggle.

<div class="row">
  <div class="col col--6">
    <p>
      To update these additional settings, click on the gear wheel, then select
      the configuration options in the pop-up window.
    </p>
  </div>
  <div class="col col--6">
    <div class="card">
      <div class="card__image">
        <img
          src={WCDEdExProp}
          alt="Properties additional details edit section"
        />
      </div>
      <div class="card__body">Properties additional details edit section.</div>
    </div>
  </div>
</div>
<br />

#### Configure nested properties

The `Object` property type can have nested sub-properties. To add nested properties, follow these steps:

<div class="row">
  <div class="col col--4">
    <ol>
      <li>Name the property.</li>
      <li>
        Set the type to <code>Object</code>. The editor displays an{' '}
        <code>Edit</code> button.
      </li>
      <li>
        Click the <code>Edit</code> button.
      </li>
    </ol>
  </div>
  <div class="col col--8">
    <div class="card">
      <div class="card__image">
        <img src={WCDEdObj} alt="Properties additional details edit section" />
      </div>
      <div class="card__body">Properties additional details edit section.</div>
    </div>
  </div>
</div>
<br />

When you finish adding nested properties, click the collection name to return to the collection editor.

#### Delete a property

To delete a property, click the trashcan icon next to it.

## Modify a collection

To modify a collection, first [select the collection](#collection-details) you want to edit.

<div class="row">
  <div class="col col--4">
    <p>
      To add a new top-level property to the collection, enter the name and type
      in the editor. Update additional configuration details if the property
      type supports them.
    </p>
    <br />
    <p>
      To add a new top-level property to the collection, enter the name and type
      in the editor. Update additional configuration details if the property
      type supports them.
    </p>
  </div>
  <div class="col col--8">
    <div class="card">
      <div class="card__image">
        <img src={WCDEdSelect} alt="Modify a collection in Weaviate Cloud" />
      </div>
      <div class="card__body">Modify a collection in Weaviate Cloud.</div>
    </div>
  </div>
</div>
<br />

## Delete a collection

Once a collection is deleted, it cannot be restored. The collection schema and all of the collection objects are deleted from the cluster. This operation cannot be undone.

To delete a collection:

import DeleteCollection from '/developers/wcs/img/weaviate-cloud-delete-collection.png';

<ol>
  <li>
    To begin, select the <code>Collections</code> tool from the left sidebar (
    <span class="callout">1</span>).
  </li>
  <li>
    The <code>Clusters</code> panel lists the clusters that are available.
  </li>
  <li>
    Select a cluster from the list to create a new collection or to see the
    collections that are already in the cluster (<span class="callout">2</span>
    ).
  </li>
  <li>
    Click on the trashcan icon to delete the collection (
    <span class="callout">3</span>).
  </li>
</ol>

<div class="row">
  <div class="card">
    <div class="card__image">
      <img src={DeleteCollection} alt="Delete a collection in Weaviate Cloud" />
    </div>
    <div class="card__body">
      <p>Delete a collection in Weaviate Cloud.</p>
    </div>
  </div>
</div>
<br />

import DeleteCollectionConfirm from '/developers/wcs/img/weaviate-cloud-delete-collection-confirm.png';

<div class="row">
  <div class="col col--6">
    <ol start="5">
      <li>
        Enter the collection name in the text box to confirm, then press{' '}
        <code>Delete</code> to remove the collection.
      </li>
    </ol>
  </div>
  <div class="col col--6">
    <div class="card">
      <div class="card__image">
        <img src={DeleteCollectionConfirm} alt="Confirm collection deletion" />
      </div>
      <div class="card__body">Confirm collection deletion.</div>
    </div>
  </div>
</div>
<br />

## Additional considerations

Weaviate collection schemas are highly configurable and can be tuned in many ways. Some schema elements are modifiable after you create the collection, but many properties cannot be modified. For additional details, see [Collection definition](/developers/weaviate/config-refs/schema).

## More resources

- [Reference: Collection definition](/developers/weaviate/config-refs/schema)
- [Starter guide: Collection definitions (schemas)](/developers/weaviate/starter-guides/managing-collections)

## Support

import SupportAndTrouble from '/_includes/wcs/support-and-troubleshoot.mdx';

<SupportAndTrouble />
