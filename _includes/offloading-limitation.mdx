:::info Offloading: AWS S3 only
As of Weaviate `v1.26.0`, tenants can only be `OFFLOADED` to cold storage to AWS S3 through the `offload-s3` module.

Additional storage options may be added in future releases.
:::
