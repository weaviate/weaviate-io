export default function URL({query}) {
  let url = `https://console.semi.technology/console/query#weaviate_uri=https://demo.dataset.playground.semi.technology&graphql_query=${query}`
  return (
    <span>
      <p class="playground-demo online" style={{fontSize: 0.85 + 'rem', marginTop: 0.25 + 'rem'}}>
        🟢 <a href={url} target="_blank" rel="nofollow">Try out</a> this GraphQL example in the Weaviate Console.
      </p>
      <p class="playground-demo offline" style={{display: 'none', fontSize: 0.85 + 'rem', marginTop: 0.25 + 'rem'}}>
        🟠 Whoops, the demo service is temporarily offline.
      </p>      
    </span>
  );
}

<URL/ query={props.query}>
