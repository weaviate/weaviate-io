:::note Cluster downtime

In most cases, a high availability (HA) cluster remains available during an upgrade. Weaviate introduces an improved cluster synchronization mechanism in v1.25. There is a service outage when you upgrade an HA cluster to 1.25 while the cluster switches to the new mechanism.

:::