import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs groupId="languages">
<TabItem value="py" label="Python">

```python
listing_collection = client.collections.use("Listing")

response = listing_collection.query.near_text(
    query="AirBnB near a place to walk my dog",
    limit=3,
)

for obj in response.objects:
    print(obj.properties["description"])
```
</TabItem>

<TabItem value="js" label="TypeScript">

```ts
const listings = client.collections.use('Listing');

const response = await listings.query.nearText('AirBnB near a place to walk my dog', {
  limit: 3,
  returnProperties: ['description'],
});

for (const obj of response.objects) {
  console.log(obj.properties.description);
}
```

</TabItem>
</Tabs>
