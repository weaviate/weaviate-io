The Python client v4 and TypeScript client v3 use [gRPC](/developers/weaviate/api/grpc). The gRPC protocol is sensitive to network delay. If you encounter connection timeouts, adjust the timeout values for initialization, queries, and insertions.