{
  "name": "ts-code-samples",
  "description": "This file exists to enable type: module in the code samples directory",
  "type": "module",
  "scripts": {
    "test": "for file in howto/*.ts; do node --loader=ts-node/esm $file; done"
  }
}
